version: '3.7'

services:

  db:
    image: postgres:12-alpine
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: some_user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: some_db